// Empty module placeholder
// Used to replace @mediapipe/selfie_segmentation import
// We use tfjs runtime instead, so this import is never actually used

// Export a dummy SelfieSegmentation class that will never be instantiated
export class SelfieSegmentation {
  constructor() {}
}

export default SelfieSegmentation;
